<%= form_for(@idea) do |f| %> 
  <%= f.text_field :title %>
  <%= f.text_area :description %>
  <%= f.label :project %>
  <%= f.collection_select :project_id, Project.all, :id, :name, {include_blank: true} %>
  <%= f.submit %>
<% end %>
